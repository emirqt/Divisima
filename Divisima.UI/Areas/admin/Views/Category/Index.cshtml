@model IQueryable<Category>

<div class="container body">
      <div class="main_container">
        

        <!-- page content -->
        <div class="right_col" role="main">
          <div class="">

            

            <div class="row">
              <div class="col-md-12 col-sm-12 ">
                <div class="x_panel">
                    <div class="x_title">
                                <h2>Kategori Yönetimi</h2>
                                <a asp-controller="Category" asp-action="New" class="btn btn-success float-right">Yeni Kategori</a>
                                <div class="clearfix"></div>
                        </div>
                            
                  <div class="x_content">
                      <div class="row">
                          <div class="col-sm-12">
                            <div class="card-box table-responsive">
                    <p class="text-muted font-13 m-b-30">
                    </p>
                    <table id="datatable" class="table table-striped table-bordered" style="width:100%">
                      <thead>
                        <tr>
                          <th>Kategori Adı</th>
                          <th>Görüntülenme Sırası</th>
                          <th></th>
                          <th></th>
                        </tr>
                      </thead>

                          @foreach (Category c in Model)
                          {
                              <tr>
                                  <td>@c.Name</td>
                                  <td>@c.DisplayIndex</td>
                                  <td><a asp-controller="Category" asp-action="Edit" asp-route-id="@c.ID" class="btn btn-warning">Düzenle</a></td>
                                  <td><a asp-controller="Category" asp-action="Delete" asp-route-id="@c.ID" class="btn btn-danger">Sil</a></td>
                              </tr>
                          }
                          

                    </table>
                    
                    
                  </div>
                  </div>
              </div>
            </div>
                </div>
              </div>
            </div>
          </div>
        </div>
        <!-- /page content -->

        
      </div>
    </div>